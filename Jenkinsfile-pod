pipeline {
    agent any

    stages {
        stage('helm deploy') {
            steps {
                sh 'cd /var/jenkins_home/workspace/nginx'
                sh 'helm list'
                sh 'helm install nginx-chart2 nginx2/ --values nginx2/values.yaml'
                
            }
        }
    }
}
